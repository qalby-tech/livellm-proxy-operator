apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: llmproviders.proxy.livellm.ai
spec:
  group: proxy.livellm.ai
  names:
    kind: LLMProvider
    listKind: LLMProviderList
    plural: llmproviders
    singular: llmprovider
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: LLMProvider is the Schema for the llmproviders API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LLMProviderSpec defines the desired state of LLMProvider
            properties:
              apiKeyRef:
                description: APIKeyRef references a Secret containing the API key
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                required:
                - key
                - name
                type: object
              baseUrl:
                description: Optional base URL for the provider
                type: string
              blacklistModels:
                description: Models to blacklist
                items:
                  type: string
                type: array
              provider:
                description: Provider type (e.g., openai, google, anthropic)
                type: string
              refreshInterval:
                description: 'RefreshInterval specifies how often to reconcile the provider status Default: 5m'
                type: string
            required:
            - apiKeyRef
            - provider
            type: object
          status:
            description: LLMProviderStatus defines the observed state of LLMProvider
            properties:
              message:
                description: Message contains any error or status message
                type: string
              lastSyncedGeneration:
                description: LastSyncedGeneration is the generation of the object that was last successfully synced
                format: int64
                type: integer
              registered:
                description: Registered indicates if the provider is successfully
                  registered with the proxy
                type: boolean
            required:
            - registered
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
